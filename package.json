{
  "name": "kegbot-server-frontend-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Frontend (browser JS) unit tests for kegbot-server.",
  "type": "module",
  "scripts": {
    "test": "vitest run"
  },
  "devDependencies": {
    "jsdom": "^24.0.0",
    "vitest": "^1.6.0"
  }
}
